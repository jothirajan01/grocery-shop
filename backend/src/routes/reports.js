const router=require('express').Router();const prisma=require('../config/prisma');const {authenticate,authorize}=require('../middleware/auth');router.use(authenticate,authorize('ADMIN'));
router.get('/sales',async(req,res,next)=>{try{const from=req.query.from?new Date(req.query.from):new Date(new Date().setHours(0,0,0,0));const to=req.query.to?new Date(req.query.to):new Date();to.setHours(23,59,59,999);const sales=await prisma.sale.findMany({where:{saleDate:{gte:from,lte:to},status:'COMPLETED'},include:{items:{include:{product:true}},customer:true},orderBy:{saleDate:'desc'}});res.json({sales,summary:{count:sales.length,total:sales.reduce((s,x)=>s+Number(x.grandTotal),0),gst:sales.reduce((s,x)=>s+Number(x.gstTotal),0)}})}catch(e){next(e)}});module.exports=router;
